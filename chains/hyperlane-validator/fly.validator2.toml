app = 'anc-hor-validator2'
primary_region = 'ord'

[build]
  dockerfile = 'Dockerfile'

[[mounts]]
  source = 'anchor_validator2_data'
  destination = '/data'

[[vm]]
  size = 'shared-cpu-1x'
  # See fly.validator1.toml's comment — same OOM/restart-exhaustion root
  # cause confirmed independently for this machine too.
  memory = '512mb'
