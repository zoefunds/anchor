node_modules/
.next/
.env
.env.local
dist/
*.log
.DS_Store
__pycache__/
*.pyc
.pytest_cache/
tsconfig.tsbuildinfo

# Rust / Solana / Foundry build artifacts
target/
out/
cache/
broadcast/


# Solana/Anchor local artifacts
.anchor/
test-ledger/

# Hyperlane relayer runtime state (RocksDB) — not source, regenerates on restart
chains/hyperlane-relayer/db/
.vercel

# Never commit real private keys, whatever they're named — solana-keygen
# keypair files (JSON arrays of a 64-byte secret key) in particular.
*-keypair.json
solana-attestor*.json
*.private-key.json

# Scratch build output (e.g. a generated PDF export and its build
# script/intermediate page images) — not source, regenerable, never
# meant to be committed.
/output/
/tmp/
